

function isConnectedTo() {
  const l = ['a', 'b', 'c'];
  return l.find(i => i === 'x');
}

function removeEdge(p1, p2, faction, deleteEmptyVertex = 'x') {
  console.log(deleteEmptyVertex);
}

removeEdge('a', 'a', 'a', 'xxxx')